---
name: 01 Basic Docker Build
# simplest workflow possible

on:
  push:
    branches:
      - jwh-github-actions
  pull_request:

jobs:
  build-image:
    name: Build Image
    runs-on: ubuntu-latest
    steps:
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Docker build
      #   uses: docker/build-push-action@v4
      #   with:
      #     push: ${{ github.event_name != 'pull_request' }}
      #     tags: hubbsy642/jekyll-serve:latest,hubbsy642/jekyll-serve:01

      - name: executing ssh to remote server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          command_timeout: 30m
          script: |
            echo "Nice Job!!!"

      # - name: install ssh keys
      #   # check this thread to understand why its needed:
      #   # https://stackoverflow.com/a/70447517
      #   run: |
      #     install -m 600 -D /dev/null ~/.ssh/git-actions
      #     echo "${{ secrets.SSH_KEY }}" > ~/.ssh/git-actions
      #     ssh-keyscan -H ${{ secrets.HOST }} > ~/.ssh/known_hosts
      # - name: connect and pull
      #   run: ssh ${{ secrets.USERNAME }}@${{ secrets.HOST }}
      # - name: do something
      #   run: echo "Boom Baby!"
      # - name: cleanup
      #   run: rm -rf ~/.ssh

      # - name: Configure SSH
      #   run: |
      #     mkdir -p ~/.ssh/
      #     echo "$SSH_KEY" > ~/.ssh/staging.key
      #     chmod 600 ~/.ssh/staging.key
      #     cat >>~/.ssh/config <<END
      #     Host staging
      #       HostName $SSH_HOST
      #       User $SSH_USER
      #       IdentityFile ~/.ssh/staging.key
      #       StrictHostKeyChecking no
      #     END
      #   env:
      #     SSH_USER: ${{ secrets.USERNAME }}
      #     SSH_KEY: ${{ secrets.KEY }}
      #     SSH_HOST: ${{ secrets.HOST }}

      # - name: do some shit
      #   run: echo "Boom BAby!"

      # - name: executing remote ssh commands using password
      #   uses: appleboy/ssh-action@v1.0.0
      #   with:
      #     host: ${{ secrets.HOST }}
      #     username: ${{ secrets.USERNAME }}
      #     password: ${{ secrets.PASSWORD }}
      #     port: 22
      #     script: whoami
